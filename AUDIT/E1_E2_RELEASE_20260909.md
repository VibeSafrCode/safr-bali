# E1 / E2 publication and release checkpoint — 2026-09-09

Founder explicitly authorized pushing and deploying both stages. This entry
supersedes earlier local-only publication status, not historical test evidence.

## Git publication

- E1 initial source: `998595adad5d3ef30085cf24f9f25c6aef108e47`, PR #1,
  branch `codex/audit-20260905-e1`, base `codex/safrway-stabilization`.
- E2 initial source: `66c2d1ca7784ac453e9e2ed32d4ab950c82b37dc`, PR #2,
  branch `codex/audit-20260905-e2`, initially stacked on E1.
- Both branches pushed. Neither stage is deployed at this checkpoint.

## Exact-source CI and corrective change

Initial push runs `34357360644` / `34357360676` and corresponding PR runs
failed frontend browser gates. Backend/PostgreSQL, bot and reference parity
passed. E2 React browser: 121 passed, 8 opt-in screenshot skips. E2 Astro:
132 passed, 4 opt-in screenshot skips, one failure in immediate native Home
anchor-to-country navigation without JavaScript. The same failure reproduced
in all four Linux runs; local current Chrome had passed earlier.

Corrective change: CSS `@media (scripting: none)` disables animated native
scrolling only when scripting is disabled. Existing rapid no-JS navigation test
now also asserts computed `scroll-behavior: auto`; no sleeps, forced clicks,
retries or removed assertions. Normal JS-enabled smooth behavior and E2's
editorial instant scrolling remain unchanged. Six targeted local browser
iterations passed. New exact-source Linux CI is still required before activation.

## E1 production gates

Independent consumer review: PASS for the current-deployment inventory, with
residual unknown external/dormant/ad-hoc callers. Runtime source, six SAFR
unit/timer definitions and root cron found no privileged Points HTTP clients.
Available backend journal interval September 2–9 contained 228,106 parsed HTTP
requests and zero privileged Points requests. No raw logs or personal data
are retained here. Routes remain publicly reachable through API Nginx subject
to service authentication; this is not a private-only network boundary.

Cloudflare gate remains OPEN: deployed real-IP prerequisites cannot be inferred
from the tunnel credential or successful IPv4 traffic. Need control-plane proof
of no visitor-IP removal, no Pseudo IPv4 overwrite and no relevant Workers
rewriting the trusted header. Read-only access or explicit confirmation was
requested; do not expose tokens. Backend/proxy activation remains held.

## Preserved production boundary

Code remains `d9f23291dffe00b40c6b5ef350664ad82b33e0f3`, clean checkout;
Astro and React roots still use the existing `fe5cf2c` release. Backend, bot,
Nginx and tunnel are active; Nginx configuration validates. No production
mutation, migration, customer message or price mutation performed.

E2 may deploy as independently verified Astro-only artifacts once its corrected
CI passes. Do not advance backend checkout, React, systemd or Nginx as part of
that static release. Preserve the old Astro symlink target for atomic rollback.
Record actual artifact digest, activated revision and public verification after
deployment; the old local E2 digest does not describe the corrected artifact.
Unrelated native WIP, protected documents and visual artifacts remain excluded.
